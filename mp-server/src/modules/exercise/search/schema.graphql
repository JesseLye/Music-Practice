type fullExerciseDetails {
    id: ID
    exerciseName: String
    exerciseSections: [SectionsWithBpms]
    createdAt: Date
    status: Status!
}

type Query {
    fullExerciseDetails(id: ID!): fullExerciseDetails
}